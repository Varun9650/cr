import 'package:flutter/material.dart';
import 'Pickup_Management/views/pickup_management_screen.dart';

class PickupManagementEntry extends StatelessWidget {
  const PickupManagementEntry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PickupManagementScreen();
  }
}
